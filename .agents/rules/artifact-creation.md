# Artifact Creation

- Do not create throwaway files in source folders.
- Put agent-only working artifacts under `.agents/created_artifacts`.
- Create project files only when the task requires them.
- Delete temporary files before finishing.
